import React from "react";

const PageSectionDescription = ({ description }: { description: string }) => {
  return <p className="sm:text-lg text-gray-500 text-center">{description}</p>;
};

export default PageSectionDescription;
